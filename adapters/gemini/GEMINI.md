# Team Collab Protocol Pointer

This is a thin pointer. If this repository is a team project, follow repo-root `AGENTS.md` and the installed `team-collab-protocol` skill from `https://github.com/dadwadw233/team-collab-skills`.

Strong signals: `obsidian-docs/`, team-collab references in `AGENTS.md`/`CLAUDE.md`, or user requests about handoff, checkpoint, shared docs, TODO ownership, Feishu automation, project audit, or docs normalization.

## Context budget

Read only `obsidian-docs/CURRENT.md`, `RISKS.md`, `NEXT.md`, and `TODO.md` before normal substantive work. Do not load the full playbook, all handoffs, all dev records, or the whole Obsidian vault unless the task specifically needs them.

Use `/handoff <topic>` or `$handoff <topic>` for end-of-session handoff. Use `/checkpoint` or `$checkpoint` for mid-session state snapshots.
